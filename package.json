{
  "name": "agentshield",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "AgentShield — Autonomous AI Agent Security Evaluation Platform",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "node server/index.js",
    "start": "npm run build && node server/index.js",
    "dev:all": "concurrently \"vite\" \"node server/index.js\""
  },
  "devDependencies": {
    "vite": "^8.2.0"
  },
  "dependencies": {
    "concurrently": "^10.0.4",
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
